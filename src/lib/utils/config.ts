// This file maintains backward compatibility
// New location: src/lib/shared/config.ts
// ConfigManager is now re-exported from the shared module

export { ConfigManager, config } from '../shared/config';